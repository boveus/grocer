<%= "#{@user.name}'s Pantry'" %>
<br />
<br />
<% @ingredients_list.each do |ingredient| %>
<%= "#{ingredient.amount_for_pantry(params[:id])} #{ingredient.name_and_measurement}" %>
<br />
<%= link_to "delete", user_pantry_pantry_ingredient_path(@user, @pantry,
@pantry.pantry_ingredients.find_by(pantry_id: @pantry.id,
ingredient_id: ingredient.id)), :method  => :delete %>
<br />
<% end %>

<%= render partial: "ingredient_form" %>


<%= link_to "Back home", user_home_path(@user.id) %>
