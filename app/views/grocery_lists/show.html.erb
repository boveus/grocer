<h4> <%= @grocery_list.title %> </h4>
<% @grocery_list.list.each do |ingredient, number| %>
  <%= "#{number} #{ingredient}" %>
  <br />
<% end %>

<%= link_to "Back home", user_home_path(@user.id), class: 'link' %>
