<h4> <%= "#{@user.name}'s recipes" %> </h4>
<% @recipes.each do |recipe| %>
<%= link_to " #{recipe.description}", user_recipe_path(@user.id, recipe.id), class: 'link' %>
<br />
<%= link_to "delete", user_recipe_path(@user.id, recipe.id), :method => :delete, class: 'link' %>
<br />
<% end %>
<%= link_to "New recipe", new_user_recipe_path(@user.id), class: 'link' %>
<br />
<%= link_to "Back home", user_home_path(@user.id), class: 'link'%>
